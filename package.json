{
  "name": "hobbit-quest",
  "version": "0.1.0",
  "private": true,
  "description": "A modern web port of the 1982 Melbourne House text adventure, The Hobbit.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
